// Simple placeholder analytics functions
// These functions do nothing but prevent errors
// Replace with real Google Analytics implementation when ready

export const trackEvent = (action: string, category?: string, label?: string, value?: number) => {
  // Placeholder - does nothing for now
  // console.log('Track event:', action, category, label, value);
};

export const trackPageView = (url: string) => {
  // Placeholder - does nothing for now
  // console.log('Track page view:', url);
};

export const initGA = () => {
  // Placeholder - does nothing for now
  // console.log('Initialize Google Analytics');
};